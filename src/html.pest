WHITESPACE = _{ " " }
text = { ASCII_ALPHANUMERIC* }
elementName = { ASCII_ALPHANUMERIC+ }
element = {
    NEWLINE*
    ~ "<" ~ elementName ~ ">"
    ~ NEWLINE*
    ~ (element | text)
    ~ "</" ~ elementName ~ ">"
    ~ NEWLINE*
}
