WHITESPACE = _{ " " | "\t" | "\r" | "\n" }
text = @{ ASCII_ALPHANUMERIC+ }
elementName = @{ ASCII_ALPHANUMERIC+ }
element = {
    "<" ~ elementName ~ ">"
    ~ (element | text)*
    ~ "</" ~ elementName ~ ">"
}

html = _{
    SOI
    ~ element
    ~ EOI
}